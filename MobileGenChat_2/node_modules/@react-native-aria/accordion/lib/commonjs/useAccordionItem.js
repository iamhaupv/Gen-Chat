"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useAccordionItem = void 0;

const useAccordionItem = (state, props) => {
  const {
    toggleItem
  } = state;
  const {
    value,
    isExpanded,
    isDisabled
  } = props; // Generate unique IDs for each accordion trigger and region

  const buttonId = `accordion-button-${value}`;
  const regionId = `accordion-region-${value}`;

  const toggle = () => {
    toggleItem(value, isDisabled);
  };

  return {
    regionProps: {
      'id': regionId,
      'aria-labelledby': buttonId,
      'role': 'region'
    },
    buttonProps: {
      'id': buttonId,
      'aria-controls': regionId,
      'aria-disabled': isDisabled,
      'aria-expanded': isExpanded,
      'onPress': toggle,
      'role': 'button'
    },
    isExpanded
  };
};

exports.useAccordionItem = useAccordionItem;
//# sourceMappingURL=useAccordionItem.js.map