import type { GSConfig, Tokens } from '../types';
type GSTokens<T> = T extends keyof GSConfig['tokens'] ? keyof GSConfig['tokens'][T] : string;
/**
 *
 * @param tokenScale Token scale ex: colors, space, fontSizes, etc
 * @param token Token name ex: primary200, red500, 1, 2, sm, etc
 * @returns
 */
export declare const useToken: <T extends keyof Tokens>(tokenScale: T, token: GSTokens<T>) => any;
export {};
//# sourceMappingURL=useToken.d.ts.map