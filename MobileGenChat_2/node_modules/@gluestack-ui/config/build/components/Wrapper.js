import React from 'react';
import { Box, Center } from '@gluestack-ui/themed';
import { StyledProvider, useColorMode } from '@gluestack-style/react';
import { createProvider } from '@gluestack-ui/provider';
import { config } from './GluestackUIProvider/config';
const Provider = createProvider({ StyledProvider });
Provider.displayName = 'Provider';
const Wrapper = ({ children, ...props }) => {
    const colorMode = useColorMode();
    return (
    // @ts-ignore
    React.createElement(Box, { dataSet: { 'theme-id': `withGluestackStyle_${colorMode}` } },
        React.createElement(Provider, { colorMode: "light", config: config, style: { flex: 1, height: '100%' } },
            React.createElement(Box, { sx: {
                    _ios: {
                        h: '100%',
                    },
                }, ...props },
                React.createElement(Center, { height: "100%" }, children)))));
};
export default Wrapper;
