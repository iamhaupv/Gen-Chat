import { useFocusManager } from '@react-native-aria/focus';
export const useTabs = () => {
  const focusManager = useFocusManager();
  const onKeyDown = e => {
    switch (e.code) {
      case 'ArrowRight':
        {
          e.preventDefault();
          //@ts-ignore
          focusManager === null || focusManager === void 0 || focusManager.focusNext({
            wrap: true
          });
          break;
        }
      case 'ArrowLeft':
        {
          e.preventDefault();
          //@ts-ignore
          focusManager === null || focusManager === void 0 || focusManager.focusPrevious({
            wrap: true
          });
          break;
        }
    }
  };
  return {
    tabProps: {
      onKeyDown,
      role: 'tab'
    },
    focusManager
  };
};
//# sourceMappingURL=useTabs.js.map